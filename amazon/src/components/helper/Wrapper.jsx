const Wrapper = props =>{
    return props.childern
    //the above holds the info passed between the opening
    //  and closing tags of any custom components
    // makes the page moree efficient and avoid jsx rendering limitations
}

export default Wrapper;